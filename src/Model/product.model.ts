export class Product{
    constructor(
        public id?: number,
        public name?: string,
        public category? : string,
        public description? : string,
        public price?: number)
        //this ? in the above means that this is an optional not mandatory
    {
        


    }
    
}